<%= form_with(model: tarea, local: true) do |form| %>
  <% if tarea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tarea.errors.count, "error") %> prohibited this tarea from being saved:</h2>

      <ul>
      <% tarea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :imagen %>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.label :subida %>
    <%= form.date_select :subida %>
  </div>

  <%= form.hidden_field :user_id, value: current_user.id %>

  <div class="field">
    <%= form.label :listo %>
    <%= form.check_box :listo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
